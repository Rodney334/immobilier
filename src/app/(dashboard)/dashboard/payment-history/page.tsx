import { PaymentHistoryClient } from "./PaymentHistoryClient";

export const metadata = { title: "Historique paiements" };

export default function PaymentHistoryPage() {
  return <PaymentHistoryClient />;
}
